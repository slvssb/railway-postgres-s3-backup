{
  "name": "railway-postgres-s3-backup",
  "version": "0.0.1",
  "description": "A simple Bun app to backup PostgreSQL databases to an S3 bucket",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "author": {
    "name": "Selva",
    "url": "https://github.com/selva"
  },
  "scripts": {
    "build": "bun build --compile --minify --sourcemap --target bun ./src/index.ts --outfile dist/server"
  },
  "dependencies": {
    "signale": "^1.4.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/signale": "^1.4.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
